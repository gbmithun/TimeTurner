<!DOCTYPE html>
<html>
<head>
  <title>Edit Blog | Admin</title>
  <link href="https://cdn.tailwindcss.com" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white p-8">
  <a href="/admin/blog-list" class="text-blue-400 underline mb-4 inline-block">Back to Blog List</a>
  <h1 class="text-3xl font-bold mb-6">Edit Blog</h1>
  <form method="POST" action="/admin/edit-blog/<%= blog._id %>" class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <input name="image" value="<%= blog.image %>" placeholder="Image URL" required class="p-2 text-black"/>
    <input name="title" value="<%= blog.title %>" placeholder="Title (h2)" required class="p-2 text-black"/>
    <input name="href" value="<%= blog.href %>" placeholder="Blog Link (href)" required class="p-2 text-black"/>
    <textarea name="content" placeholder="Content" required class="p-2 text-black md:col-span-2 h-40"><%= blog.content %></textarea>
    <button type="submit" class="col-span-full bg-blue-600 text-white px-4 py-2 mt-4 rounded">Save Changes</button>
  </form>
  <form method="POST" action="/admin/delete-blog/<%= blog._id %>" class="mt-4">
    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded" onclick="return confirm('Delete this blog?')">Delete Blog</button>
  </form>
</body>
</html>